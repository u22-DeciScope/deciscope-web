services:
  frontend:
    build:
      context: .
    command: npm run dev -- --host 0.0.0.0 --port 5193
    env_file:
      - path: .env.local
        required: false
    ports:
      - "5193:5193"
    environment:
      API_PROXY_TARGET: ${API_PROXY_TARGET:-http://host.docker.internal:9090}
      WS_PROXY_TARGET: ${WS_PROXY_TARGET:-ws://host.docker.internal:9090}
      VITE_DECISCOPE_WS_URL: ${VITE_DECISCOPE_WS_URL:-ws://localhost:5193/api/v1/ws/transcript-segments}
      VITE_DECISCOPE_API_BASE_URL: ${VITE_DECISCOPE_API_BASE_URL:-http://localhost:5193}
    volumes:
      - .:/app
      - frontend_node_modules:/app/node_modules
    networks:
      - deciscope

networks:
  deciscope:
    name: ${DECISCOPE_DOCKER_NETWORK:-deciscope}

volumes:
  frontend_node_modules:
